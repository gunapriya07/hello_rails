<% content_for :title, "New Todo" %>

<div class="container mt-4">
  <h1 class="text-center text-success mb-4">Create a New Todo</h1>

  <div class="card shadow-sm">
    <div class="card-body">
      <%= render "form", todo: @todo %>
    </div>
  </div>

  <div class="mt-3">
    <%= link_to "← Back to Todos", todos_path, class: "btn btn-secondary" %>
  </div>
</div>
